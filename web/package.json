{
  "name": "openbook-web",
  "version": "1.0.0",
  "private": true,
  "description": "Frontend tests for openbook admin.html (v4.13.0 起的 JS 测试基建)",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "vitest": "^1.6.1"
  }
}
