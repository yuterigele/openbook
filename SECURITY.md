# 安全策略

## 支持范围

安全修复优先跟进 `main` 和最新发布的稳定 Tag。开发分支、个人 Fork 和过期版本可能不再接收修复。

## 报告漏洞

请通过 GitHub 的 **Security → Report a vulnerability** 私下报告漏洞；如果仓库尚未开启 Private Vulnerability Reporting，请先联系仓库维护者，不要创建公开 Issue。

报告中请尽量包含：

- 受影响的提交、Tag 或部署方式；
- 可重复的最小步骤、请求样例或测试用例；
- 影响范围，例如跨商户/顾客访问、重复预约、未授权工具调用或秘密泄露；
- 已知的缓解措施。

不要在报告、Issue、Pull Request 或日志附件中提交真实 API Key、企业微信密钥、数据库密码、JWT、手机号、顾客资料或完整生产请求体。可以使用占位符和脱敏日志。

维护者会尽力在 7 天内确认收到报告，并在修复、缓解措施和公开披露时间确定后更新报告人。高风险问题可能先发布安全修复，再补充公开说明。

## 部署方应急措施

发现凭据泄露时，应立即轮换对应凭据、暂停受影响的回调或写路径，并保留脱敏审计信息。不要只回滚应用镜像：如果新版本已经执行数据库迁移，还必须按备份、反向迁移和数据核验流程处理。

OpenBook 的重点安全边界包括租户/顾客归属、渠道验签和重放保护、Agent 工具白名单、写操作幂等与并发保护，以及生产配置中的密钥和弱密码检查。
