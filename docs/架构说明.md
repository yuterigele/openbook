# OpenBook 架构图

> 项目定位：顾客使用普通微信说一句话完成预约。企业微信仅提供商家侧“客户联系 / 微信客服”接入能力，顾客无需安装企业微信。

## 1. 运行时请求链路

第一张图只描述“消息如何进入 Agent”；数据落库、回复与人工闭环见下一张图。拆分后在 Markdown 预览中可直接阅读，无需缩放整张图。

```mermaid
flowchart TB
    customer["顾客（普通微信）"]
    friend["企业微信好友会话"]
    kf["微信客服链接 / 二维码"]
    web["本地 Web Demo"]
    callback["企业微信回调\n验签、解密、去重、多店路由"]
    ingress["入口保护\nRedis 限流 → 规则预筛\n可选 Qwen Flash 分类"]
    reject["高风险 / 无关输入\n只回复预约范围说明"]
    session["会话隔离\n顾客 + 门店"]
    trusted["服务端可信上下文\nshop_id + 顾客身份 + 北京时间"]
    agent["Eino ADK Booking Agent\n敏感检查 → 意图识别 → 工具规划\nDeepSeek → OpenAI → Ark\n单 Provider 30s；整轮 60s\n可恢复错误最多重试 2 次"]

    customer --> friend --> callback
    customer --> kf --> callback
    web --> ingress
    callback --> ingress
    ingress -->|通过| session --> trusted --> agent
    ingress -->|拦截| reject
```

```mermaid
flowchart TB
    agent["Agent 工具调用"]
    query["查询工具\n排班、理发师、服务、节假日"]
    booking["预约工具\n创建、查询本人、取消本人"]
    handoff["转人工\n当前：仅写审计事件"]
    mysql["MySQL\n门店、顾客、预约、服务、事件"]
    redis["Redis\n限流、预约分布式锁"]
    breaker["Redis 健康熔断\n连续 3 次失败：只读\n连续 3 次成功：恢复写入\n切换/恢复：飞书告警"]
    reply["安全的场景化回复"]
    wecom["企业微信客户联系 / 微信客服 API"]
    mock["Demo mock\n仅记录事件"]
    queue["待实现：工单队列与店员通知"]
    human["待实现：人工处理并按原会话回推"]

    agent --> query --> mysql
    agent --> booking --> redis
    redis --> breaker
    booking --> mysql
    agent --> handoff --> mysql
    handoff -. 目标闭环 .-> queue -.-> human
    agent --> reply
    reply --> wecom
    reply --> mock
    human -.-> wecom
```

> 虚线和“待实现”节点表示建议补齐的人工交接闭环，并非当前代码已有能力。当前 `handoff_to_human` 只写 `event_logs`，不会自动通知店员或把人工处理结果异步发回顾客。

> Redis 进入只读保护后，查询工具仍可用；创建预约、取消预约和转人工会被拒绝，防止在锁不可用时继续写入。Redis 初始化失败、切入只读和恢复写入都会向已配置的 `FEISHU_ALERT_WEBHOOK_URL` 发送通知。



## 2. 安全与部署边界

```mermaid
flowchart TB
    internet["普通微信顾客 / 店员后台"]

    subgraph host["开发机 · Docker Compose"]
        app["app 容器\nGo + Hertz + Eino Agent\n127.0.0.1:38080"]
        mysql["mysql 容器\n127.0.0.1:3306"]
        redis["redis 容器\n127.0.0.1:6379"]
        bootstrap["db-bootstrap 一次性服务\n创建受限 openbook 数据库账号"]
        app -->|"受限账号\nSELECT / INSERT / UPDATE / DELETE\n迁移所需 DDL"| mysql
        app --> redis
        bootstrap --> mysql
    end

    internet -. "企业微信回调需经公网反向代理 / TLS\n当前本地 Demo 不直接暴露" .-> app

    subgraph trust["不可跨越的信任边界"]
        userinput["不可信：用户文本、模型工具参数"]
        trusted["可信：服务端回调身份、shop_id、会话上下文"]
        policy["工具层强制校验\n门店隔离 + 顾客归属 + 事务/锁"]
        userinput --> policy
        trusted --> policy
    end

    app --- policy
```





## 3. 演示时的讲解顺序（60–90 秒）

1. 顾客从普通微信进入，再分流到店员企业微信好友会话或微信客服会话。
2. 两种入口在服务端统一成会话与可信身份上下文，并按门店隔离。
3. 限流后先做确定性输入相关性/风险评分；可选 Qwen Flash 只处理模糊分类，失败立即回退规则。提示注入、命令、广告、乱码和长无关文本只得到范围提示，不消耗 Agent 或工具资源。
4. Agent 只做自然语言理解和工具规划；关键词未命中时由可选小模型补充意图标签。创建预约由 Redis 时段锁和 MySQL 事务/唯一约束共同保护，回调去重之外仍有业务层并发防护。
5. 模型按 DeepSeek、OpenAI、Ark 尝试；单 provider 默认 30 秒，整轮默认 60 秒。可恢复错误（429、5xx、网络超时）最多重试 2 次，再切换；启动时全部 provider 不可用则只回复“AI 助手暂不可用”，不写业务数据。
6. 转人工目前仅留审计埋点；上线前应接入队列/工单、店员通知及原会话异步回推，形成可追踪闭环。
7. 安全上，顾客 Agent 没有 Shell、文件、任意 SQL 或后台权限；查询/取消预约必须校验门店、身份与归属。
8. 本地 Demo 使用 Compose 和 mock 回复；部署公网时才需要额外配置反向代理、TLS、密钥托管与企业微信回调地址。



## 4. 图中真实代码入口

- 消息入口与双通道：`server/server.go`
- 输入相关性/风险阈值：`server/input_trust.go`
- Qwen Flash 小模型分类适配：`chatmodel/small.go`、`server/input_trust_llm.go`
- 限流：`server/ratelimit.go`
- Agent 编排与工具白名单：`internal/agent/agent.go`
- 模型超时、运行时切换与降级回复：`chatmodel/model.go`、`chatmodel/fallback.go`
- 预约 Redis 时段锁与事务落库：`tools/create_appointment.go`、`storage/repo.go`
- 当前转人工限制（仅埋点）：`tools/handoff_to_human.go`
- 顾客预约归属校验：`tools/get_appointment.go`、`tools/cancel_appointment.go`
- 事务取消策略：`storage/cancel_policy.go`
- Docker 与受限数据库账号：`docker-compose.yml`
